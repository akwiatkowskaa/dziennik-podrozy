@model Trip
@{
    ViewData["Title"] = "Nowa podróż";
    var isAdmin = ViewBag.IsAdmin as bool? == true;
}
<div class="page-header">
    <h2>Dodaj podróż</h2>
    <a asp-action="Index" class="btn btn-outline-secondary btn-sm">← Wróć do listy</a>
</div>

<div class="card shadow-sm form-card">
    <div class="card-body">
        <div asp-validation-summary="All" class="alert alert-danger" role="alert"></div>

        <form asp-controller="Trips" asp-action="Create" method="post">
            <div class="row g-3">
                <div class="col-12">
                    <label class="form-label">Tytuł podróży</label>
                    <input asp-for="Title" class="form-control" placeholder="np. Weekend w Gdańsku" />
                    <span asp-validation-for="Title" class="text-danger small"></span>
                </div>
                <div class="col-md-6">
                    <label class="form-label">Data od</label>
                    <input asp-for="DateFrom" type="date" class="form-control" />
                </div>
                <div class="col-md-6">
                    <label class="form-label">Data do</label>
                    <input asp-for="DateTo" type="date" class="form-control" />
                </div>
                <div class="col-12">
                    <label class="form-label">Opis <span class="text-muted">(opcjonalnie)</span></label>
                    <textarea asp-for="Description" class="form-control" rows="3" placeholder="Możesz zostawić puste"></textarea>
                </div>
                <div class="col-md-6">
                    <label class="form-label">Kraj</label>
                    <select asp-for="CountryId" asp-items="ViewBag.CountryId" class="form-select"></select>
                    <span asp-validation-for="CountryId" class="text-danger small"></span>
                </div>
                @if (isAdmin)
                {
                    <div class="col-md-6">
                        <label class="form-label">Właściciel podróży</label>
                        <select asp-for="UserId" asp-items="ViewBag.UserId" class="form-select"></select>
                    </div>
                }
                else
                {
                    <input type="hidden" asp-for="UserId" />
                    <div class="col-md-6 d-flex align-items-end">
                        <p class="text-muted small mb-0">Podróż zostanie zapisana na Twoim koncie.</p>
                    </div>
                }
            </div>
            <div class="mt-4">
                <button type="submit" class="btn btn-primary">Zapisz podróż</button>
                <a asp-action="Index" class="btn btn-secondary">Anuluj</a>
            </div>
        </form>
    </div>
</div>
